export type PosterType = {
  id: number;

  image: {
    uri: string;
    alt: string;
  };

  title: string;
  colorTitle: string;
  petName: string;
  petSex: string;
  registerDate: string;
  local: string;
};
